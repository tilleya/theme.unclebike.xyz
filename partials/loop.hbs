<article class="feed {{post_class}}{{#if noDate}} archive-wrapper{{/if}} post-year-{{date format="Y"}} post-date-{{date format="M"}}" data-month="{{date format="MMMM YYYY"}}">
    
    {{#if noDate}}
        <div class="date-wrapper">
            <div class="section-title year-post-label">{{date format="YYYY"}}‘s {{t 'Thoughts'}}</div>
            <div class="section-title month-post-label"> ✦ {{date format="MMMM"}}</div>
        </div>
    {{/if}}

    {{#match @custom.blog_article_layout "!=" "Text-only"}}
        {{#match @custom.blog_article_layout "!=" "Minimal"}}
            {{#if feature_image}}
                <div class="feed-image u-placeholder{{#match @custom.blog_image_orientation "Square"}} square{{else}}{{#match @custom.blog_image_orientation "Portrait"}} portrait{{/match}} rectangle{{/match}} round">
                        <img class="u-object-fit"
                            srcset="{{img_url feature_image size="xs" format="webp"}} 150w,
                                    {{img_url feature_image size="s" format="webp"}} 400w,
                                    {{img_url feature_image size="m" format="webp"}} 750w"
                            sizes="(min-width: 576px) 300px, 90vw"
                            src="{{img_url feature_image size="s"}}"
                            alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                            loading="lazy"
                        >
                </div>
            {{/if}}
        {{/match}}
    {{/match}}
    <div class="feed-wrapper">
        {{#primary_tag}}
            <div class="feed-tag">
                <span class="feed-tag-item sub-heading">{{name}}</span>
            </div>
        {{/primary_tag}}
        <h2 class="body-1 feed-title">{{title}}</h2>
        {{#if excerpt}}
            <div class="feed-excerpt">{{excerpt words="20"}}</div>
        {{else}}
            <div class="feed-excerpt">{{custom_excerpt}}</div>
        {{/if}}
        <div class="dot-spacer"></div>
        <div class="feed-right">
            <div class="feed-visibility feed-visibility-{{visibility}}">
                {{> icons/star}}
            </div>
            <time class="body-1 feed-calendar" datetime="{{date format="YYYY-MM-DD"}}">
                {{date published_at format="DD MMM"}}
            </time>
            <div class="feed-icon">
                {{#if noLink}}
                    {{> icons/arrow-down}}
                {{else}}
                    {{> icons/arrow-right}}
                {{/if}}
            </div>
        </div>
    </div>
    {{#if noLink}}
        <a class="u-permalink" href="#{{slug}}" aria-label="{{title}}"></a>
    {{else}}
        <a class="u-permalink" href={{#if canonical_url}}{{canonical_url}}{{else}}{{url}}{{/if}} aria-label="{{title}}"></a>
    {{/if}}
</article>